# Syntra continuous improvement loop

This is the living backlog for autonomous maintenance. Every scheduled pass
must inspect current code and evidence first, update this ordering, complete one
safe item end to end, run focused verification, and record material evidence.
It must not claim external validation from unit tests or make production writes.

## Selection rule

Choose the first item that is both incomplete and executable without new user
authority. Prefer safety and data-integrity defects over features, and prefer a
verified vertical slice over several half-built changes. A pass is complete
only when implementation, tests, documentation, and any required migration are
consistent.

## Prioritized backlog

The complete gap register is maintained in
[the enterprise-readiness backlog](enterprise-readiness-backlog-2026-09-23.md).
This shorter queue is the autonomous engineering order; external validation
and organizational decisions stay in the full register until their evidence
exists.

1. Add job cancellation states and cooperative cancellation checks for long-running imports, syncs, and provisioning runs.
2. Add cursor pagination for long operation timelines and remaining high-volume governance lists (the audit log is done), and move the audit page's chain verification onto the checkpointed incremental walk.
3. Add live-region status updates to every remaining asynchronous admin action.
4. Run the security workflows in GitHub and resolve real CodeQL, dependency, secret, and container findings.
5. Complete manual accessibility, onboarding, pilot, and go/no-go validation with the user, including a restore drill of a backup that predates a tenant deletion.

## Completed recently

- Provision Snipe-IT user accounts through the document-driven HTTP connector.
  - **Refusals in a 200 body.** New optional `failures.body` rule (`at`, `equals`, `messageAt`, `conflictWhen`, `notFoundWhen`; plain case-insensitive substrings, schema-validated and bounded): a 2xx whose body matches is a failure — `conflict`, `not_found` or `rejected` — on writes, list pages, the connection test and read-back. The surfaced message is the target's own `messages`, flattened, cut to 300 characters and passed through `scrubText`, with the vault credential and the initial password removed by value. Before this a refused Snipe-IT create was recorded as a success.
  - **Offset paging held to a total.** Optional `paging.totalAt`: the walk continues until the stated total rather than stopping on the first short page (Snipe-IT silently caps `limit` at `max_results`, which would have returned page one as the whole directory), and a page that runs dry before the total throws.
  - **Single-object read-back.** Optional `account.read` (`GET /users/{{anchor}}`); the HTTP connector now has `readBack`, which uses it when declared and falls back to the shared enumeration otherwise (`readBackByEnumeration` / `completeReadBack` extracted from `readBackTarget`, behaviour unchanged). `capabilitiesForTarget` advertises read-back for a document with no entitlements when an enabled-state field is mapped.
  - **The document.** `documents/snipe-it.ts`, offered as "Snipe-IT" in the console (credential labelled *Personal API key*, a warning until `{instance}` in `baseUrl` is replaced): users only; anchor `id`, correlation `username`, provenance `employee_num`; create/update/rename/deactivate/reactivate; no archive or delete. Explicit `User-Agent` for Cloudflare error 1010. Docs: `docs/connectors/snipe-it.md`.
  - **Verification.** `http/snipe-it.test.ts` against `testing/fake-snipe-it.ts` (offset paging under a capped page size, create with `payload.id`, idempotent adoption, 200 "already been taken" → conflict with a redacted message, other 200 errors → rejected, update, deactivate/reactivate, not-found, single-GET read-back, the shared certification runner users-only); new body-rule and `totalAt` cases in `http/connector.test.ts`; a console test that the Snipe-IT button fills the document. Not verified against a live Snipe-IT instance.

- Finish onboarding against accounts another run created, stop empty receipt previews blocking a target, and show operators what is held for their review (found live on an Entra target with `autoApply` on).
  - **Read-back instead of manual work.** A person receipt whose preview planned nothing for the person, while the evaluated rules require an account, used to finish `verification_pending` ("Confirm the observed account…") without reading the target. It now reads the linked account back once (`verifyReceiptAtTarget`, the check the apply path uses; once because nothing was written) and finishes `applied` when the account and entitlements match what Syntra records, so the lifecycle operation's target step succeeds and the operation completes. A mismatch, an incomplete or unsupported read-back, a missing anchor/credential or a read error stays `verification_pending` with the reason.
  - **Empty receipt previews are closed.** A preview a receipt started that holds no actions and no exceptions for anybody is closed `applied` (`closeEmptyPreviewedRun`, audited as `provision.run.apply` with `emptyPlan: true`; no target checks because nothing is written, `lastAppliedRunAt` untouched) instead of staying `previewed` and refusing every later receipt and scheduled run as "awaiting review". Blocked runs, runs with actions or exceptions, and non-receipt runs are unchanged. **Deliberately left:** a receipt preview that holds actions only for OTHER people stays `previewed` for a person to apply (or for the next run to supersede); it now appears in the attention banner. A receipt that applies its own person's actions with `only` already leaves the run terminal (`applied`, or `partially_applied` when other people's actions were left proposed).
  - **Attention.** `GET /api/admin/attention/summary` (session only; each section gated on the caller's own permissions: `provision.read` for runs held for review and lifecycle operations failed or awaiting read-back verification, an approver permission for pending privileged change requests; `null` when not readable). A console-wide banner (`AttentionBanner`, `role="status"`, polite, polls every 60 s, dismissible for the session until something new arrives) names the target, what the run would do and links to it and to Activity → Attention, which now lists the same runs under "Waiting for a decision". A run held by a threshold names the setting ("Accounts created", 20%, this run measured 50%) and links to the target's Safety thresholds section (`#safety-thresholds`); a first-run hold says no setting changes it.
  - **Verification.** `person-receipts.readback.test.ts` (fake Graph + real Entra connector: account created by a separate run → receipt `applied` by read-back, operation `completed`, empty run closed, a second receipt not refused; mismatch and incomplete read-back stay `verification_pending`; work for another person stays `previewed`), `health/attention.test.ts`, the attention route test in `incidents.test.ts`, `AttentionBanner.test.tsx`, run-page threshold-hint tests; person-receipts, run-service, apply, jobs, lifecycle, incidents, lifecycle-operation and OpenAPI suites; full web suite; `tsc -b`, lint, `openapi:check`, `privacy:inventory:check`. No schema change.

- Correlate observed Entra ID, SCIM and HTTP accounts by their own key, not `sAMAccountName` (found on a live install: an Entra create refused as "userPrincipalName is taken" could not be adopted — the candidate lookup answered `candidate-not-visible` with base-DN advice).
  - **One helper.** `observedCorrelationKey(targetType, config, record)` in `packages/core/src/provision/observed-key.ts` (beside `correlationAttributeFor`, moved there from `apply.ts`) is now the only place an object read from a target becomes a key: `sAMAccountName` on AD (unchanged), `userName` on SCIM, the document's mapped field on HTTP, and on Entra ID the UPN's local part only when its domain is the one `entraUserPrincipalName` would complete a key with — any other domain returns the whole UPN, which can never equal an `@`-less key. Used by the run's observed objects (which feed the taken-key set), the adoption candidate search and in-flight create resolution; all three spelled `sAMAccountName` or the raw UPN before, so every Entra/SCIM object read as nameless.
  - **Safety unchanged.** Reconcile binds on the anchor only, so a matching name never makes an unmanaged account managed: as on AD, an in-domain user reserves its name and the person is proposed the next free one; a create Graph refuses as taken is a `conflict`, and only adoption (candidate shown, reason required, audited) binds it.
  - **Messages.** `CandidateNotVisibleError` names no base DN on a flat target: "no account named X is visible in the target".
  - **Updater.** `syntra-update <version>` for a version not newer than the running one now refuses without writing `var/update.status`, so the console keeps showing the previous success; it still exits non-zero with the reason.
  - **Verification.** `observed-key.test.ts`; `flat-target.test.ts` extended against the fake Graph/SCIM (in-domain user reserves the key and is never written; other-domain user does not; a collision is a conflict, the next run leaves the person alone, the candidate is found by local part, adoption binds it and the following run proposes no create; GUID-tenant adoption by local part; other-domain refusal without base-DN advice; SCIM `userName` reserves its key); a new `syntra-update.test.sh` case. AD adoption and run suites unchanged and passing. Docs: `docs/connectors/entra-id.md` *Existing users and adoption*.

- Make native Entra ID and SCIM targets able to create accounts through a provisioning run (found on a live install).
  - **Container placement is declared.** `TargetConnector.placesAccountsInContainers(config)` is a new required member: Active Directory `true`, Entra ID and SCIM `false`, a document-driven HTTP target `true` only when its document describes `container`. Previously the run read `listContainers`, which is empty for every flat target, and dropped every person as `container_missing`, so nothing could ever be created. The run, reconcile and planner now skip the container check (and `container_vanished`, container creation and container-change moves) only for a target that declares itself flat; Active Directory is unchanged, and an empty list from it is still `container_missing` for every person (Ruling P9). A manual move, the container list and org-unit container mapping answer `409 no-containers` on a flat target; the profile preview reports no container problem for one. Certification now checks the declaration (a placing target must list at least one container).
  - **Entra UPN domain.** New optional `userPrincipalDomain` on the Entra config (connector and contracts schemas: lowercase domain, no `@`, scheme or path). `principalName()` precedence: a key with `@`, then `userPrincipalDomain`, then a domain `tenantId`, else a refusal naming the setting. The account-profile preview shows the full UPN and reports a problem before apply when none can be formed. Not part of the transport: credential borrowing ignores it.
  - **SCIM credential.** Found by the end-to-end test: the SCIM connector read its token only as `bearerToken`, but core hands every target its vault credential as `bindPassword`, so every SCIM request through a real run went out as `Bearer undefined`. It now accepts either.
  - **Console.** Entra target form: **User principal name domain** with help text. Target detail: the Configuration panel sits directly below Connection and warns when the target has no account profile or no enabled rule grants an account. Account profile: container settings are explained as ignored for Entra ID and SCIM; the preview shows the UPN and "not used" for the container of a flat target.
  - **Verification.** New `packages/core/src/provision/flat-target.test.ts` drives profile, rule, preview and apply through the registry's real Entra and SCIM connectors against the fake Graph and fake SCIM servers (GUID tenant plus `userPrincipalDomain` → `POST /users` with `anna.novak@contoso.com`; SCIM creates with no `container_missing`). Unit tests for UPN precedence and config validation, reconcile/run-service/placement/API/web tests, the AD `container_missing` regressions, the full TypeScript build, lint and the OpenAPI check pass. Docs: `docs/connectors/entra-id.md`.

- Add queue recovery controls, customer-safe status reporting and an operational support bundle (backlog #57, #63, #64).
  - **Job health (#57).** `packages/core/src/jobs/job-health.ts` compares every non-terminal sync run, HR import run, provisioning run, person target operation, export and lifecycle operation with pg-boss's own table (read directly, filtered on the payload's `tenantId`) and the clock, and reports `orphaned` (no live job for 10 minutes, or a provisioning apply whose one-minute heartbeat is 15 minutes old), `stuck` (6 hours, the adoption threshold), `duplicated`, `delayed` (15-minute queue SLA), `poisoned` (3+ attempts of one payload in 24 h, with an error class and never the message) and `saturation_deferred`. An unreadable queue reports nothing orphaned.
  - **Repairs.** `requeue`, `mark_failed` and `release_lease`, each offered only where safe and reusing the subsystem's own semantics: a requeue only where no worker started (claims are conditional), mark-failed as the abandoned-run path does it (a waiting cancellation is honoured instead), and release closes a dead provisioning apply as `partially_applied` keyed on the heartbeat value it read, leaving `in_flight` actions for `resolveInFlightActions` before the next plan. No repair re-runs a connector write; lifecycle operations and sync/HR applies are reported, not repaired. Every repair re-derives the finding, is idempotent (`noop`), and is audited (`job_health.*`).
  - **Status (#63).** `GET /api/admin/status` (`audit.read`) separates shared components (API, database, queue, key provider, SMTP via a new optional `Transport.verify`) from this tenant's own write stops, stale or failing readiness, connector outages (with error classes) and job findings, and never the queue depth. `GET /api/admin/deployment/status` (`deployment.manage`) adds the release, redacted probes, migrations, queue depth, missing schedules and installation-wide counts, naming no tenant.
  - **Support bundle (#64).** A `support_bundle` export kind (`tenant.manage`) through the secure export service, window at most seven days and fixed at request: software and migration state, a tenant settings fingerprint, configuration fingerprints, write stops, connector readiness, job health, recent failures by error class and audit counts. Built by allow-list, then passed through the shared log redaction; a test seeds secrets and personal data into every table it reads and asserts none survive, including in the downloaded file.
  - **Console, metrics, alerts, docs.** System → Operations (status, background work with reasoned repairs and a live region, support bundle); `syntra_job_health_findings{kind,finding}` and `syntra_job_queue_readable`, with six rules in `ops/prometheus-alerts.yml` (Helm copy identical); operate.md *Queue recovery*, *Status reporting* and *Support bundles*, and a queue-recovery runbook. Migration `20261103101500_support_bundle_export_kind`; focused core, API and web tests, the OpenAPI coverage test, the full TypeScript build and lint pass.

- Add credential lifecycle, the security notification policy and the in-product certificate/key expiry inventory (backlog #34, #52, #67).
  - **Inventory.** `GET /api/admin/credentials` (and **Settings → Credentials**) computes one tenant-scoped list from the tables that hold each credential: target, directory-source and HR-feed secrets, pinned SFTP host keys, upstream client secrets and signing certificates, service-provider certificates, SAML/OIDC signing keys, API tokens, webhook signing secrets and OIDC client secrets. Each entry has its expiry and where it came from (`certificate`, `issued`, `discovered`, `declared`, `none`, `unknown`), last rotation (vault write time, certificate `notBefore`, key or token creation), owner, note and status. `CredentialRecord` (FORCE RLS) keeps only what cannot be computed; nothing ever holds a secret, vault name or digest.
  - **Entra discovery, gated.** A new optional `readCredentialExpiry` capability (`Application.Read.All`) reads the registration's own `passwordCredentials` and matches the held secret by Graph's three-character hint; a 403 is `not_permitted`, not an error, and is not retried for a week. No new permission is required for anything else.
  - **Alerts.** A daily `credentials.expiry_scan` job (and **Scan now**) raises the most urgent crossed threshold (tenant-configurable, default 30/14/7/1) and the expiry, once per expiry date, with a conditional claim so concurrent replicas raise it once. Each is a `credential.expiring`/`credential.expired` security event (Credentials webhook group) and a mail to the owner, plus `tenant.manage` holders when opted in or when there is no owner. An expired credential is a critical `credential_expired` incident.
  - **Rotation.** `CredentialRotation` (FORCE RLS, one open per system by partial unique index, database CHECKs that a cut-over carries a passed test and a closed rotation holds no sealed secret): stage the new secret beside the live one, verify it with the connector's own test against the saved configuration, cut over (refused after a day or after any configuration change; old secret kept, token caches dropped), complete (live secret re-tested before the old is erased, readiness check recorded) or roll back. Every step is audited and appended to the rotation's evidence.
  - **Security notification policy (#52).** An audit of what already emitted found three gaps, now closed: an HR-feed credential change was not audited at all, target/source/upstream credential replacements were not security events, and signing-key rotation was not audited. `credential.changed` and `signing_key.rotated` now are, `person_source.host_key_accepted` joined the Credentials group, and a new **Data exports** webhook group carries `export.request`/`download`/`revoke` (the #48 remainder). Six categories (`SECURITY_NOTIFICATION_CATEGORIES`, rendered as the policy table in configure.md) can additionally email `tenant.manage` holders per a new tenant setting, from `recordEvent` in the same transaction; privileged role grants, administrator MFA resets and SAML-only key rollovers are narrowed by a qualifier. Mail carries no audit payload and is never digested. Break-glass use has no dedicated event (none exists); documented.
  - **Verification.** Migration `20261102090000_credential_lifecycle`. New core tests (`credentials.test.ts`, `security-policy.test.ts`), connector test (`entra/credential-expiry.test.ts` against the fake Graph), API test (`credentials.test.ts`), web tests (`CredentialsTab.test.tsx`, `SecurityAlertsTab.test.tsx`); related notify, keys, health, jobs, provision, export, source, OpenAPI, webhook and scheduler suites pass. 13 new admin routes described; `docs/api/openapi.json` regenerated (331 operations).
- Add separation of duties for privileged administrative changes and the break-glass (emergency access) lifecycle (backlog #31 and #32).
  - **Separation of duties.** A tenant chooses which privileged change classes are held for a second administrator (Settings → Change control). Four are implemented end to end: privileged role grants (assigning a role with a privileged permission, and widening a role to one), admin-scoped API token minting, authentication-policy relaxation (any weakening `PUT /tenant`; tightening is never held) and webhook endpoint create/update. A held route answers `409 change-approval-required` until given a reason in `X-Syntra-Change-Reason`, then stores a revision-bound `PrivilegedChangeRequest` and answers `202`; the console prompts and resends for every form. A different administrator holding the class's permission approves from a ten-minute step-up session, and approval applies the change in the same transaction through the same code the direct route runs (secrets go to the approver once). A stale revision invalidates the request; 72-hour expiry; the requester may only withdraw; switching a class off is itself held. The database enforces four eyes, decision completeness and step-up freshness. Federation configuration and target credentials are documented as not yet covered; the generic handler mechanism is how they adopt it.
  - **Break-glass.** A different, stepped-up administrator designates an emergency account and receives a sealed 256-bit recovery credential once (digest only stored). Outside an activation the account is refused administrative scope by `authorize()` (and tokens acting as it always are). Activation is requested without a session (`POST /api/auth/break-glass/activate`, one generic refusal, rate limited), mailed at once to every `tenant.manage` holder and fanned out as a security event, and takes effect after the tenant's delay (15 min–24 h, default 60) unless cancelled, or earlier on a different administrator's stepped-up approval. The account then elevates through `authorize()` with only the WebAuthn-console requirement lifted, into a session that lives exactly as long as the activation (15–240 minutes, automatic expiry, early end by anyone). A different, stepped-up administrator must complete a written post-event review; a console banner shows every administrator pending and active activations and outstanding reviews. Threat model and the interaction with `adminWebauthnRequired` are in configure.md.
  - **Notifications.** A new **Privileged access** webhook group carries every change-request and break-glass transition; a once-a-minute sweep closes expired requests and activates or expires activations with events and mail.
  - **Verification.** Migration `20261101143117_privileged_change_break_glass` (FORCE RLS on three tables, CHECK constraints for four eyes, step-up freshness, bounded windows and review completeness). New core tests (`privileged/change-control.test.ts`, `privileged/break-glass.test.ts`, 20 tests), API tests (`change-control.test.ts`, `break-glass.test.ts`, 10 tests), web tests (`ChangeControlTab.test.tsx`, `BreakGlassTab.test.tsx`, 12 tests), plus the focused authorize, session, webhook-group, OpenAPI, roles, tokens, webhooks, tenant, auth, scheduler and database-invariant suites, the full TypeScript build and lint pass.
- Build the data inventory and data-subject request workflows (backlog #69 and #70, engineering slices).
  - **Inventory (#69).** `packages/core/src/privacy/inventory.ts` classifies every column of all 135 models (identity, contact, hr, authentication, audit, operational, or not personal) with each area's purpose, source, retention, access and a legal-basis placeholder, and says per linked table whether an erasure pseudonymises, deletes or retains its rows and why. `inventory.test.ts` fails when a Prisma model gains an unclassified column, links through a column that does not exist, or when `docs/privacy/data-inventory.md` (generated by `pnpm privacy:inventory`) is stale.
  - **Cases (#70).** `PrivacyCase` (reason, identity-verification method and attestation, received and due dates, 30 days by default and at most 90), with the audit log as its timeline. Search reads every inventory-linked table; the access bundle is a new `dsar_bundle` export kind (sealed, watermarked JSON carrying the inventory's descriptions, credential material excluded); rectification is the ordinary person and contract edit with `privacyCaseId`; restriction (`Person.processingRestrictedAt`) makes provisioning withhold writing and widening actions (kept on the plan as `refused`, naming the case) and HR imports and directory sync skip such changes, while disable, archive, revoke, deactivate, departures and contract ends still run; erasure is refused on a legal hold (holds now accept `subjectType: person`, and retention honours them), unresolved lifecycle or in-flight provisioning work, an active person, or live accounts or grants, and otherwise needs a second administrator from a fresh session (database four-eyes check), pseudonymises in place, deletes credential material, retains the audit log and decisions, erases any bundle file, makes the restriction permanent and keeps a digest-bound receipt. New permission `privacy.manage` (backfilled onto built-in `tenant.manage` roles); erasure routes refuse machine tokens. Console: **Directory → Privacy requests**, the case page with its timeline, and a link from the person page.
  - **Verification.** Migration `20261104090000_privacy_data_subject_requests`. Core: `privacy/inventory.test.ts`, `privacy/dsar-service.test.ts` (refusals, four-eyes, step-up, a row-by-row scan of every tenant table proving no identifying value survives outside the audit log, tenant isolation, the bundle), `privacy/restriction.test.ts` (HR import, sync, provisioning). API: `routes/admin/privacy.test.ts`. Web: `PrivacyPage.test.tsx`, `PrivacyCasePage.test.tsx`. Documented in [Operate, Data-subject requests](operate.md#data-subject-requests).
  - **Left.** Legal-basis values and the DPIA (#13, #79) are the controller's decisions; audit payloads naming an erased person leave on the audit-retention archive-and-prune schedule; target systems' own copies must be erased there.
- Extend the tenant-isolation probe (backlog #39) to the credential, change-control, break-glass, operations and privacy slices, and fix what the second pass found.
  - **Coverage.** The probe now seeds 62 kinds of object and walks 408 routes: 243 all-foreign id probes, 96 mixed probes, 47 body probes and 89 list probes. It also runs 25 job handlers, including `privileged.access_sweep` and `credentials.expiry_scan`. New checks:
    - Every jsonb column is included in the scan for A rows that hold B ids.
    - A refused or no-op write must not audit a success naming B.
    - B's break-glass credential is refused at A's host.
    - A's support bundle and DSAR access bundle contain nothing of B.
  - **Defects fixed.**
    - Seven idempotent routes audited a success for a removal that removed nothing, naming whatever foreign ids were sent. A's DSAR bundles therefore carried B's role ids. The routes are role unassign, group member removal, policy rule delete, `DELETE /api/auth/mfa/webauthn/:credentialId` (which also mailed the caller that a key had been removed), admin factor removal, and per-user session revoke. Removals now report a count and audit only a real removal. The WebAuthn, factor and session routes answer 404 for an id this tenant does not hold.
    - `SodException.basisContractIds` (JSON, so no foreign key at all) stored another tenant's contract ids, and the lapse sweep then ended the exception at once. The basis must now be contracts of the exception's person.
    - Onboarding committed its lifecycle operation, carrying the foreign ids, before checking the org unit, the manager and the targets. Another tenant's target was silently dropped from an onboarding reported as started. All of them are now checked before the operation is recorded.
    - A Govern CSV export queued any `snapshotId` and failed later in the job; the snapshot is now looked up first.
    - SCIM answered `application/scim+json` with 415 (RFC 7644 §3.1). The SCIM plugin now parses it with Fastify's JSON parser; covered in `scim.test.ts`.
  - **Verification.** `tsc -b` and lint pass. The probe (22 tests) passes, as do the focused API and core suites for every touched route and service.
  - **Left.** Job health reads pg-boss, which is not in the test database (`queueReadable: false`). Its tenant filter on the payload's `tenantId` is therefore reviewed by hand but not probed.
- Build the tenant-isolation test suite (backlog #39): a generated cross-tenant probe over every route and background job, which found and fixed real defects.
  - **The probe.** `apps/api/src/tenant-isolation/` seeds two tenants with one row of each of 55 kinds of object. Acting as tenant A's all-permission administrator, it walks `app.routeCatalog`: 366 routes (all 305 `/api/admin`, the portal, SCIM, and the SAML routes that take an application id). Each run makes 219 all-foreign id probes, 95 mixed probes (one foreign parameter at a time, or A's path with a foreign body), 43 body probes with bodies generated from the published OpenAPI schemas, and 79 list probes. It also runs all 23 registered pg-boss handlers with A's tenant and B's ids. The checks are: a refusal matching the status for an id that never existed; no B tag or id in any response; B's rows byte-identical after every write; a refused write leaves A unchanged; and no A row holds a B id. Structural tests fail on an unclassified path parameter, an undecided parameterless write, a job without a payload, or a stale allow-list entry.
  - **Isolation defects fixed.** Row-level security does not cover foreign-key references, because PostgreSQL checks a foreign key without the referenced table's policies. Ten write paths could store another tenant's id. The probe caught most of them directly. An audit of the same pattern found the rest: `User.orgUnitId` through onboarding, contract create, and the mover.
    - `Person.orgUnitId` (create and PATCH)
    - `Contract.managerPersonId` (create, PATCH and the mover)
    - `User.orgUnitId`
    - `AppAssignment` subject
    - `BusinessFunction.ownerPersonId`
    - `SodRule.functionA/BId` and the exception workflow
    - `Campaign.ownerPersonId`
    - `GovernFinding.ownerPersonId` (assign)
    - `ResourceOwner` resource and owner
    - `ResourceDelegation` resource and delegate

    Each now goes through `assertReferenceInTenant` in core (`tenant-reference.ts`), an RLS-scoped lookup whose failure is a 404 that names the field.
  - **Routes that ignored a path parameter.** Three routes ignored one of their own path parameters. `DELETE /applications/:id/assignments/:assignmentId` and `DELETE /applications/:id/claims/:claimId` deleted the child under any application's path and audited the wrong application. `POST /person-import-runs/:runId/changes/:id/skip` never read `:runId`. All three are now scoped by their parent. `POST /govern/sources/:kind/:id/refresh` queued a job for any id and reported it enqueued; it now answers 404.
  - **Wrong answers that still failed closed.** About 30 routes answered another tenant's id with a 500: Prisma `P2025`, Govern's `SnapshotNotReadableError`, the sync and HR run services' "no such source", a 502 from target containers, and placement moves. `problem-json` now maps `P2025` to 404 and `SnapshotNotReadableError` to 404 or 409, and the remaining routes look the id up first.
  - **Verification.** `tsc -b` and `pnpm lint` pass. The probe and `problem-json.test.ts` pass, as do the focused suites over the touched services and routes.
  - **Not covered.** Protocol endpoints other than the application-id routes, and the oidc-provider catch-all, are left to their own suites. Some body probes still stop at validation before the lookup (22 × 400 in a run); `PROBE_LOG` lists them. A database-level same-tenant reference check is proposed under #40.
- Make the API correct at more than one replica, closing the two per-process defects the Helm multi-replica audit found (chart 0.3.0, default `api.replicas: 2`).
  - **OIDC provider cache.** `Tenant.oidcConfigGeneration` is bumped by database triggers in the same transaction as every change a cached `oidc-provider` instance is built from: `OidcClient` insert/update/delete (the admin route, catalog installs, which previously invalidated nothing, and cascades), OIDC `SigningKey` rotation and retirement (the worker's job included), and tenant hostname changes. SAML key changes and unrelated tenant settings do not bump it. Each process's cache records the generation it was built at and rebuilds when the tenant row, which every OIDC request already reads, is newer, or when the issuer differs. That gives zero staleness, no extra query, and no `LISTEN/NOTIFY`, so it is PgBouncer-safe. `invalidateProvider` and the key-change listener remain as local fast paths only.
  - **Rate limits.** A Postgres store for `@fastify/rate-limit` (`RateLimitBucket`, one `INSERT … ON CONFLICT … RETURNING` per limited request, using the database clock, plus a bounded sweep every minute) is the default (`RATE_LIMIT_STORE=postgres`; `memory` is kept for single-process installs). It keeps the default store's fixed-window semantics, `continueExceeding`, and separate counters per route and per limiter. `perTenantRateLimit` takes an explicit scope, and a duplicate scope is refused at startup. `/health/ready` fails open on store errors so it can still report a database outage as 503. Other limited routes fail closed.
  - **Docs.** The chart README, `values.yaml`, NOTES, `operate.md` and `configure.md` drop the caveats, the rate-limit-division advice and the restart workaround. The monthly rolling restart is now described as optional.
  - **Verification.** Migration `20261027173100_replica_safe_state`. New `replica-state.test.ts` uses two independent provider caches over the production deps to prove a client change, a direct-table client insert, a key rotation and retirement, a client delete and a hostname move made through one are served by the other on its next request. It also uses two app instances over one database to prove the per-address and per-tenant limits are global, and it proves the counter's window, `continueExceeding` and sweep semantics. The full TypeScript build, focused lint, `helm lint --strict` for the minimal and full values, and focused OIDC, rate-limit, auth, tenant, SCIM, federation, deletion and migration tests pass.
- Publish a versioned OpenAPI 3.1 description of the administration and machine-token API (backlog #99, foundation).
  - **Coverage.** All 305 `/api/admin` routes are described. That includes the 11 that refuse tokens, which are published as session-only.
  - **Where it lives.** It is served unauthenticated and tenant-free at `GET /api/openapi.json`, and committed as `docs/api/openapi.json` by `pnpm openapi:generate`.
  - **Facts derived, not written.** The method, path, required permissions (`x-syntra-permission`), whether a token may call the route (`x-syntra-token-allowed`) and its rate limit all come from the running route table. An `onRoute` catalog reads the guards themselves: `requirePermission` records what it checks, and Govern's scoped guard declares its permissions.
  - **Per-module descriptions.** Summaries and the contracts Zod schemas each handler already parses with sit beside every route module in `routes/admin/<module>.openapi.ts`. They are converted by a small Zod-3 walker that defers to Zod 4's `z.toJSONSchema` once that upgrade lands.
  - **Shared shapes.** The document carries the bearer and cookie security schemes, the RFC 9457 problem and validation-problem shapes, and the paging parameters and envelope.
  - **Guards against drift.**
    - A test fails on any undescribed or stale route. Its explicit allow-list for deliberate exclusions is commented and empty.
    - A CI `openapi document` job regenerates the file and fails on any difference.
    - The six-month deprecation notice is enforced by test and announced with RFC 9745/8594 `Deprecation`/`Sunset` headers.
  - **Also fixed.** Reusing an onboarding `idempotencyKey` with different input escaped as a bare 500. It is now `409 idempotency-key-reused`, and nothing is written.
  - **Conventions.** `docs/api/README.md` documents versioning (only additive changes within v1), deprecation, errors, idempotency, rate limits and client generation.
  - **Verification.** Focused API and core tests (21 files, 458 tests), the full TypeScript build and lint pass.
- Add capability enforcement and certification-aware adapter rollout (backlog #18 and #33). The connector lifecycle catalog now holds releases per adapter, each with a channel and a per-version certified-capability list; the registry maps each release to the implementation this build ships, and a test keeps the two in step. Every planned connector action is checked at preview against the exact release and the target's advertised capabilities (for `httpJson`, its document) and re-checked at apply; a refused action stays in the plan with status `refused` and an operator-readable reason, is excluded from guard arithmetic and never attempted, keeps any Govern revocation order behind it open, fails a linked access-request item with the reason, and the run records its adapter version, refusal count and summary, shown as a banner on the run page. Targets carry a `stable`/`canary` channel and optional exact pin; a selection change records the certified release it left as the rollback point, and an audited rollback pins it again touching only the selection — configuration, profile, rules, placements and accounts are unchanged, and a run previewed under another release refuses to apply. Deprecated, partially certified or uncertified releases are readiness warnings (adapter panel, setup checklist, readiness API); from the deprecation date new writes stop (preview blocked outright, apply and manual move refused) unless an audited override bound to that release and at most 30 days long (database-enforced) is active. Permission-checked API and a target-page Adapter release panel; four new audit events in the Configuration changes webhook group. The SCIM capability table's stale, previously display-only `manageEntitlements: false` was then corrected to match the implemented, certified grant/revoke, so enforcement does not refuse SCIM grants. Migration `20261028091500_adapter_capability_rollout`; focused connectors, core, API and web tests, the full TypeScript build, and focused lint pass.
- Add structured redaction and end-to-end tracing (backlog #56 and #55). One rule set (`packages/connectors/src/observability/redact.ts`) decides what is a secret (passwords, tokens, cookies, authorization headers, client secrets, private keys, SAML assertions, vault plaintext, TOTP secrets, recovery codes) and what is personal (email, names, UPN, phone, DN, employee id, person records) for logs and span attributes alike, scrubs free text of bearer tokens, JWTs, PEM blocks, SAML XML, URL credentials, query strings, DN values and email addresses, and bounds depth, width and length. The single application logger (`apps/api/src/logging.ts`, which the scheduler also uses) layers a safe error serializer — HTTP-client errors reduced to method, query-less URL and status so an attached `Authorization` header never reaches the walker — a redacting formatter, message scrubbing and pino `redact` paths, and stamps every line with a correlation id. Every request and job runs under a 32-hex correlation id returned as `x-correlation-id`, carried through pg-boss payloads (stripped before handlers) so an HR import, the provisioning run it causes and its connector calls share it, and written to the new, hash-excluded, format-constrained `AuditEvent.correlationId` (filterable via `GET /api/admin/audit?correlation=`). Optional OpenTelemetry, off and never loaded unless `OTEL_EXPORTER_OTLP_ENDPOINT` is set, adds server spans (route pattern, status, tenant), job consumer spans parented across the queue, per-operation connector spans, `guardedFetch` client spans (host and status only; `traceparent` is not sent to third parties) and opt-in Prisma spans; failures are recorded as type, code and scrubbed message. Metrics labels were audited and pinned to a closed set by a test. Migration `20261029090000_audit_correlation_id`; focused connectors, core (audit, scheduler, sync, person-source, net), db, and API (logging, telemetry with an in-memory exporter, metrics, app boot, health, admin routes) tests, the full TypeScript build and lint pass.

- Add key-management integration (backlog #47). `MasterKeyProvider` gains HashiCorp Vault / OpenBao **Transit** (token or AppRole auth with single-flight login and re-login on 403, namespace support, the tenant id as authenticated `associated_data`, key versions carried in each `vault:vN:` ciphertext) and **AWS KMS** (`GenerateDataKey` for writes, `Decrypt` always naming the configured key, the tenant id as encryption context, optional via `AWS_KMS_ENCRYPTION_CONTEXT`). Rows keep the existing columns: an external provider writes a self-describing format marker where the local provider's 12-byte nonce goes, so local and KMS rows coexist without a migration and each row records whether it was tenant-bound. `MASTER_KEY_PROVIDER=local|vault-transit|aws-kms` and its variables are validated in `loadConfig` with every missing variable named; `MASTER_KEY` becomes optional and decrypt-only beside an external provider, and `MASTER_KEY_PREVIOUS` / `VAULT_TRANSIT_PREVIOUS_KEY` / `AWS_KMS_PREVIOUS_KEY_ID` give every rotation a read-both window through a fallback composite that never wraps with an old key. External providers sit behind a bounded, TTL'd, LRU unwrap cache keyed by wrapped bytes and tenant, zeroing on eviction and handing out copies; the defined outage behaviour (cached reads until TTL, uncached reads and all writes fail at once, readiness fails, password sign-in unaffected) and revocation latency are documented and tested. A new `key-management` readiness probe round-trips a random canary past the cache, remembering only passes for 30 s. One provider instance per process is shared by every route and the scheduler (routes now take `keyProvider` instead of `masterKey`), bootstrap seals with the configured provider, and startup logs the provider, its decrypt-only leftovers and whether it answered. `pnpm rekey --status|--yes` (`packages/db/src/rekey*.ts`) is the operator-run wrapper around `rewrapSecrets`: canary-checks the new provider first, one transaction per tenant, re-runnable, and records a `vault.data_keys_rewrapped` audit event with before/after counts per provider and key version. `syntra-backup` fingerprints the external key reference so post-migration restores still match. The Vault dev server is an optional `kms` compose profile (`pnpm vault:up`/`vault:wait`) started in CI, where `VAULT_TRANSIT_INTEGRATION=1` turns the integration test's skip-when-unreachable into a failure. configure.md "Key management", master-key-recovery Procedure C and secret-rotation Procedure B document variables, IAM/Vault policies, outage and revocation, who logs what (the KMS logs key access; Syntra audits rekeys and credential writes), and the local-to-KMS migration. Tests: 51 new unit tests (fake KMS that really encrypts, fake Transit over HTTP), DB-backed vault/readiness/rekey tests, 6 integration tests against a real Vault 1.20.4 dev container (tenant AAD enforced, least-privilege AppRole, token revocation, version rotation and `min_decryption_version` retirement, full local-to-Transit migration), the backup harness, the full TypeScript build, and lint pass. Not done: a LocalStack or real-AWS test (the AWS provider is exercised only against the in-memory KMS), Azure Key Vault, and the Helm backup CronJob still fingerprints only `MASTER_KEY`.
- Add the secure export service and audit search at scale (backlog #48, #73). One asynchronous path for bulk copies: `POST /api/admin/exports` records a `DataExport` row (FORCE RLS) and queues `exports.generate`; the job re-checks the requester's authority as it stands then, reads in keyset batches of 1,000 bounded at the log head, writes a watermark (export id, tenant, requesting user, time — a JSON Lines header record, or leading columns on every CSV row), records the plaintext SHA-256 and seals the file with the vault's envelope (new `createEnvelopeSealer`/`openEnvelope`) in `bytea`, refusing anything over 64 MiB. Download is requester-only, re-checks the permission and that the authority fingerprint (tenant, or the Govern org-unit scope) is unchanged, verifies the digest, sends `no-store`; expiry is 1–72 h (default 24, database-enforced), a 15-minute sweep erases expired ciphertext and fails abandoned jobs, and the requester or `tenant.manage` can revoke; database constraints refuse terminal rows that still hold ciphertext. Every step is an audit event (`export.request/ready/fail/download/revoke/expire`). The audit-log export and the Governance access CSV (`POST /govern/exports/csv`, now `202`) moved onto it; the offboarding export and evidence bundles stay synchronous for reasons in operate.md. `GET /audit` gained server-side actor, action-prefix, target, target-type, outcome and time-window filters with keyset `nextBefore` paging (cap 200), saved searches per administrator (`AuditSavedView`), and "Export these results"; four indexes back the filters, and a suite test seeds 100,000 events and asserts every plan (it caught a time-window page reading 1,986 blocks, fixed by resolving the window to a sequence range and by `recordEvent` never letting time run backwards along the chain). Console: Activity → Exports (status, download, revoke, polling with a live region), audit search filters, saved searches and keyset pager, and Export as CSV on the Governance access report. Migration `20261030120000_data_exports_audit_search`; focused core, API and web tests, the full TypeScript build and lint pass. Not done: per-page chain verification still walks the whole log; no correlation id exists to search; export-creation notifications (#52).

- Complete the external-write circuit breaker with a tenant-wide stop and notifications. A tenant-isolated (FORCE RLS) stop row shares the per-target semantics through one guard at the apply boundary: a mandatory reason, an optional expiry of at most 30 days honoured the moment it passes, and resume only by a different administrator. While active, no connector write is attempted for any target; the run stays previewed, a manual account move is refused after its placement is recorded, a scheduled `autoApply` run records a visible skip, and a person receipt is left blocked rather than failed. A once-a-minute sweep closes expired stops at both scopes exactly once with an actor-less audit event. Every pause, resume, and expiry is a security event in the new **Emergency write stops** webhook group, so subscribed endpoints are notified through the existing hash-chained audit fan-out. The target list carries a conspicuous tenant-wide control built from the same panel as the per-target one. Focused core, API, and web tests, the full TypeScript build, and focused lint pass.
- Production-grade Helm chart and HA deployment guidance (chart 0.2.0).
  - **Security defaults.** The chart never renders secrets by default and fails via `required` if no Secret is named. Pods run non-root with a read-only root, all capabilities dropped, seccomp `RuntimeDefault` and no service-account token.
  - **Workloads.** Probes use the real `/health` (liveness) and `/health/ready` (readiness) endpoints. The migration runs as a pre-install/pre-upgrade hook with a backoff limit, a deadline and a TTL. The API no longer migrates on start.
  - **Optional resources.** Ingress, HorizontalPodAutoscalers with conservative scale-down, PodDisruptionBudgets only when more than one pod can exist, topology spread, and a ServiceMonitor that uses the metrics bearer token. The PrometheusRule is a CI-enforced copy of `ops/prometheus-alerts.yml`.
  - **NetworkPolicy** is on by default: ingress only from the ingress controller, egress limited by port (DNS, Postgres, SMTP, a connector allow-list), with cloud metadata addresses excluded.
  - **Backup CronJob.** It mirrors `syntra-backup create`'s checks and layout.
  - **Multi-replica audit.** It found that the OIDC provider cache and the rate-limit counters are per process. OIDC client, hostname and key-rotation changes reach only the replica that made them. This is documented with its consequences, and an optional scheduled restart covers key rotation. (Both fixed since. See the replica-safety entry above.)
  - **PgBouncer testing.** Transaction pooling was tested against PgBouncer 1.25. It works with `max_prepared_statements` above zero or `pgbouncer=true`, and fails without either.
  - **Validation.** `helm lint --strict` and `helm template` pass for the minimal and full values, and kubeconform validates all 24 rendered resources. The overridden API and migration commands and the backup script were exercised against the release images with a read-only root filesystem. A new CI `helm` job runs lint, render, the refusal check, schema validation, the backup-script parse and the alert-copy diff.

- Add phishing-resistant console access (backlog #44). `Tenant.adminWebauthnRequired` makes `authorize()` demand WebAuthn — never TOTP, email OTP or a recovery code — for every administrative-scope decision, inside the chokepoint rather than as a caller-supplied floor; forced enrolment is withheld at elevation so a relayed password cannot register an attacker's key, and existing console sessions a key did not establish stop at their next request. Lockout prevention refuses enabling unless the caller holds a registered key and is using a key-established session, refuses it without a primary domain, and pins the domain while it is on; the break-glass procedure is documented. Dedicated `tenant.admin_webauthn_required`/`_relaxed` audit events; the elevate screen explains a missing key instead of reporting a wrong password. Migration `20261024043000_tenant_session_policy`; focused core, API and web tests, the full TypeScript build and focused lint pass.
- Add tenant-configurable session lifetimes and an administrator mass revoke (backlog #43). Idle and absolute lifetimes per scope are tenant settings within documented platform bounds (admin idle 5–60 min, admin absolute 15 min–12 h, portal idle 5 min–24 h, portal absolute 1 h–30 days, idle within absolute, admin within portal) enforced by the contract and database CHECK constraints, defaulting to the previous hardcoded values; liveness is evaluated against the current policy on every request so shortening takes effect immediately, and the inventory shows the effective expiry. `POST /api/admin/sessions/revoke` ends every session or only console sessions tenant-wide, optionally sparing the caller's, behind `tenant.manage`, a recorded reason and ten-minute elevation step-up, refusing bearer tokens; every affected person goes through `endSessions`, so refresh tokens, OIDC grants and back-channel logout follow, with per-user `session.revoked` (`mass_revoke`) and a summary `session.mass_revoked` event, batched with partial-failure evidence. Console form and revoke tab with confirmation, step-up recovery, and a live-region result.
- Add revision-bound, four-eyes tenant deletion. A `tenant.manage` request names a current offboarding assessment and a later export by digest; both record a new data revision (SHA-256 over exactly the exportable data), and any edit since makes the request refuse as stale. Active legal holds and unresolved lifecycle work refuse at request, approval and execution. A different administrator approves from a session stepped up within 15 minutes (enforced in code and by a database constraint), within 72 hours; execution opens after a 24-hour cooling-off and closes seven days later; any administrator can cancel; machine tokens are refused; every refusal is audited. Execution re-runs every check under an exclusive tenant binding lock that every `withTenant` transaction now takes shared, crypto-erases the vault, removes pg-boss schedules and queued jobs, deletes every `tenantId` table in catalog-derived dependency order (append-only decision rules disabled only inside the transaction), and leaves a scrubbed tombstone, the completed request as a personal-data-free receipt, and the immutable audit chain ending in the completion event. Also fixed: the export digest hashed every timestamp as `{}` and could not be recomputed from the downloaded file, and `LifecycleLegalHold`/`LifecycleCaseEvent` had no row-level security, so one tenant's hold was counted against another. Migration `20261025100000_tenant_deletion_execution`; console flow under Settings → Offboarding; retention, audit and backup-expiry treatment documented in operate.md. Focused core, db, API and web tests, the full TypeScript build, and focused lint pass.

1. Add a revision-bound, four-eyes tenant deletion execution path that consumes a current offboarding assessment and refuses stale exports, legal holds, or unresolved lifecycle work.
2. ~~Add asynchronous, permission-checked, watermarked exports for large reports.~~ Done (backlog #48; see Completed recently).
3. Add cursor pagination for long operation timelines and remaining high-volume audit/governance lists.
4. Add live-region status updates to every remaining asynchronous admin action.
5. Run the security workflows in GitHub and resolve real CodeQL, dependency, secret, and container findings.
6. Complete manual accessibility, onboarding, pilot, and go/no-go validation with the user.

## Completed recently

- Add cooperative cancellation for directory sync runs, HR person imports, and provisioning runs. Each run carries an explicit, constraint-checked cancellation state (`requested` → `cancelled`, or `moot` when the run finished before observing it) with requester and timestamps; an audited `POST …/cancel` action uses the same permission that applies the run. Queued and review-pending runs cancel immediately (abandoned changes are marked with the reason, open duplicate reviews close, and revocation orders a cancelled provisioning plan carried are re-opened); reading and applying runs stop at checkpoints between items — never between a provisioning action's `in_flight` marker and the target's answer — leaving every applied item recorded and the run an honest, reviewable partial. Cancelled runs cannot be applied. The three run pages offer a confirmed Cancel control with polite live status. Migration 83 applied to the scratch database; 284 focused core and database tests (13 new cancellation tests proving checkpoints stop between items), 150 focused API tests, 80 focused web tests, the full TypeScript build, and focused lint pass.
- Add a permission-checked tenant offboarding export artifact. The downloadable, versioned JSON document contains portable identity, organization, role, source, target, account, and entitlement data in stable order; it excludes vault ciphertext, password proofs, MFA and recovery material, tokens, private signing keys, and transient protocol artifacts by construction. A SHA-256 digest is returned both in the document and response header, while a permanent hash-chained audit receipt records the digest, exclusions, and per-section counts without duplicating the exported personal data into the audit log. Thirty-three focused core and API tests, the full TypeScript build, focused lint, and diff validation pass.
- Add a durable tenant-offboarding preflight. The `tenant.manage`-only API inventories key tenant record classes without decrypting secrets, refuses deletion readiness when an active legal hold or unresolved lifecycle operation exists, binds the exact assessment to a SHA-256 digest, and stores a permanent hash-chained audit receipt. No deletion path exists yet. Thirty focused core and API tests, the full TypeScript build, focused lint, and diff validation pass.
- Complete the account-profile data-minimization UI. The editor detects each personal-email template before save, names every receiving target attribute and its sensitive classification, requires a 20-character purpose locally, preserves the recorded reason, shows approval status and timestamp, and sends no incomplete approval request. The layout remains responsive and its inventory uses the shared table primitive. Nineteen focused web tests, the full TypeScript build, focused lint, and the Impeccable detector pass.
- Add the server-side data-minimization gate for target account profiles. Attribute templates that reference `person.personalEmail` are classified and blocked until the save carries a substantive purpose; accepted saves persist the reason, approving actor and timestamp, expose a structured sensitive-mapping inventory, and include the decision in audit evidence. Removing the sensitive mapping atomically clears the approval so stale purpose cannot authorize a future disclosure. The database constraint prevents partial approval evidence. Migration 82 applied successfully; 148 focused profile, API, and migration tests, the full TypeScript build, focused lint, and diff validation pass.
- Add field-level classification and least-privilege reads for private HR contact data. `personalEmail` is classified sensitive and omitted—not nulled—from person lists and detail responses unless the caller holds the new `identity.sensitive.read` permission, preventing unauthorized callers from learning whether a value exists. Personal-email correlation suggestions are protected by the same permission so the value cannot leak indirectly as a match reason; internal lifecycle/provisioning behavior remains intact. 140 focused RBAC, person, matching, and API tests, the full TypeScript build, focused lint, and diff validation pass.
- Complete the governed-reference-data console on the Sources page. Departments and locations each expose their enforcement state, responsive add form, allowed/inactive rows, enable/disable controls, accessible loading and error status, and a first-value empty state that makes the opt-in consequence explicit. Nine focused web tests, the full TypeScript build, focused lint, and the Impeccable detector pass.
- Add the server-side governed reference-data foundation for departments and locations: tenant-isolated catalogs, database kind constraints and RLS, normalized uniqueness, audited create/enable/disable APIs, and opt-in enforcement during HR preview. A field remains permissive until it has an active value; afterward, unapproved rows are withheld and recorded without turning them into false leavers. The migration applied successfully; 102 focused database, API, mapping, diff, and import tests, the full TypeScript build, and focused lint pass.
- Harden HR identity references at ingestion: duplicate employee identifiers now withhold every conflicting occurrence and protect already-owned people from false snapshot absence; manager identifiers must resolve within the source namespace or the same feed; and same-feed managers are resolved through durable source links during the ordered apply, without a second import. Focused person-source coverage (71 tests) and the full TypeScript build pass.
- Complete non-destructive duplicate-person governance: exact business-email matches block before creation under tenant RLS; the console shows incoming identities, survivor candidates, and affected person/contract changes; reviewed decisions can keep separate, skip, or create an audited reversible source link to a selected survivor. Link removal preserves both the Person and their contracts. No merge path exists.
- Durable per-target UTC maintenance-window enforcement, including cross-midnight intervals, database constraints, fail-closed configuration, target-console controls, and an audited confirmed-and-reasoned urgent-leaver-only exception surfaced directly in the run console.
- Per-target emergency external-write circuit breaker enforced before a provision run enters `applying`: audited stop reason and optional bounded expiry, a conspicuous operator-console state, no connector writes while active, and four-eyes resume by a different administrator. Reads, previews, and durable evidence remain available during containment.
- Target-specific retry limits are now visible and editable in the target console with a server-enforced range of 1–10 attempts. The connector contract uses a closed failure vocabulary: only transient and throttled outcomes retry; conflict, rejection, unauthorized, and not-found outcomes stop immediately. Exponential delay and throttle attempts/time remain platform-bounded.
- Per-target connector health history derived from durable evidence, with empty-day continuity and 7/30/90-day UTC views for connection failures, authentication failures, average/p95 latency, provisioning failures, unknown write outcomes, throttling, retries, and read-back completeness. The target editor fails closed when a rolling-upgrade API cannot supply the new contract.
- Durable lifecycle case management on the existing operation record: ownership, severity, due date, acknowledgement, automated escalation, operator notes, controlled resolution codes, reopening, actor attribution, append-only history, and audit events. The operation console exposes the full case trail and retention/legal holds preserve it with the operation.
- Audited, revision-bound document-driven Entra to native Entra migration. It preserves the target identifier, vault credential, schedule, profile, accounts, entitlements, rules, and run history; refuses non-Microsoft endpoints and stale previews; invalidates old readiness by changing the fingerprint; and is available as a preview/confirm workflow on the target page.
- Connector lifecycle catalog with adapter version, connector API compatibility, support state, rollout state, deprecation date, and contract-certification evidence. The API and target console expose the same authoritative record; Entra and HTTP remain visibly controlled/preview rather than overstated as generally supported.
- Shared connector certification runner covering connection, idempotent create/update/disable, observed-state verification, entitlement grant/revoke, and missing-object classification; native Entra, SCIM, disposable Samba/Active Directory, and document-driven HTTP fixtures pass it.
- HTTP connector documents now declare correlation and scalar/collection provenance read-back. Unsafe documents neither advertise nor execute account creation; exact action IDs are adopted and foreign collisions are refused.
- SCIM create idempotency: an exact action marker is adopted on retry, while a same-name foreign account is a conflict rather than a duplicate or silent takeover.
- Native Entra delayed read-back and manual-verification fallback.
- Explicit Graph application permissions per Entra capability.
- Verification-gated retry for ambiguous target outcomes.
- Database-backed employee work queue and 10,000-record query-plan rehearsal.
- Tenant-configurable lifecycle idempotency retention.
- Lifecycle legal holds with audited placement/release and retention enforcement.
- Legal-hold UI on operation timelines, including released-history visibility and preservation of linked receipts, observations, and delivery records.
- CI definitions for CodeQL, dependency audit/update, secret scan, SBOM, provenance, and image scan.
